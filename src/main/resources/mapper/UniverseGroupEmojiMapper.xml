<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aws.sync.mapper.UniverseGroupEmojiMapper">
    <insert id="addUniverseGroupEmojiOneByOne">
        insert into group_emoji (meeting_id, current, acc_average, time_ms)
        values(#{meeting_id}, #{current},#{acc_average},#{time_ms})
    </insert>

</mapper>