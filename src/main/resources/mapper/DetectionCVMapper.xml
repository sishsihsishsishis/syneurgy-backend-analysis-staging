<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aws.sync.mapper.DetectionCVMapper">
    <insert id="addDetectionOneByOne">
        insert into detection_cv (meeting_id, users, starts, ends, types, keyword)
        values(#{meetingId}, #{users}, #{starts}, #{ends}, #{types}, #{keyword})
    </insert>

</mapper>